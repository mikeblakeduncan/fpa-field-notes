name: Publish Weekly Blog Post
# DISABLED — schedule trigger removed April 2026 as part of site rebuild.
# Automated blog post generation has been deprecated. Do not re-add the schedule trigger.
# Keep for reference only. Run manually via workflow_dispatch if needed.

on:
  workflow_dispatch:
    inputs:
      force_week:
        description: 'Week to generate (YYYY-MM-DD, any date in that week). Leave blank to use the current week.'
        required: false
        default: ''
        type: string
      overwrite:
        description: 'Overwrite an existing post for this week if one already exists.'
        required: false
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  generate-post:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install anthropic

      - name: Generate and publish post
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          FORCE_WEEK: ${{ inputs.force_week }}
          OVERWRITE: ${{ inputs.overwrite }}
        run: python scripts/generate_post.py
